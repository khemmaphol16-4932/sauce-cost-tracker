# หลังบ้านเดลิเวอรี — รอบแรก

หน้า `/today` เป็นหน้าแรกหลังเข้าระบบ ภาษาไทย รองรับออเดอร์หลายรายการ คิวทำอาหาร เช็กแพ็ก ส่งมอบ และลงยอดขายเข้าระบบเดิม

## ติดตั้ง

1. ใช้ branch `codex/delivery-today` ซึ่งอิง GitHub commit `66a5b4e` และนำงาน made-to-order จาก commit `4f0e624` มารวมแล้ว งาน local เดิมยังอยู่ในโฟลเดอร์ `sauce-cost-tracker`
2. ตรวจให้มี migration เดิมถึง 0019 รวม `0016_made_to_order_recipes.sql` แล้วจึงรัน `supabase/migrations/0020_delivery_orders.sql` ก่อนเปิดหน้าใหม่ ตรวจประวัติ migration จริงก่อนรัน ไม่รัน 0016 ซ้ำหากติดตั้งแล้ว
3. ตั้ง `NEXT_PUBLIC_SUPABASE_URL` และ `NEXT_PUBLIC_SUPABASE_ANON_KEY` ตามโปรเจกต์เดิม เก็บค่าไว้ใน environment
4. `npm ci`, `npm run build`, และ `npm run start`

## ใช้งาน

- สร้างเมนูและราคาในหน้าเมนูอาหารก่อน ไม่ต้องมีสต็อกเพื่อรับออเดอร์เข้าคิว
- เพิ่มออเดอร์ เลือกช่องทาง แตะเมนูหลายรายการ แก้จำนวน ราคา และหมายเหตุ พร้อมเวลาที่ต้องพร้อมส่ง
- ราคาที่เติมเป็นราคากลางเดิม ยังไม่มีราคาบันทึกแยกตามช่องทาง ไม่มีการคิดค่าบริการหรือส่วนลดอัตโนมัติ
- กดเริ่มทำ เช็กอาหารทุกรายการและบรรจุภัณฑ์ก่อนกดพร้อมส่ง แล้วกดส่งให้ไรเดอร์แล้ว
- หลังส่ง กรอกค่าธรรมเนียมตามจริงแล้วกดลงยอดขายและตัดสต็อก ระบบสร้างหลายรายการขายใน transaction เดียว และเริ่มสถานะเงินเป็น pending
- หากสต็อกไม่พอ การลงยอดทั้งหมดจะไม่เกิดขึ้น ออเดอร์ยังอยู่กลุ่มส่งแล้ว / รอลงยอด ให้จัดสต็อกเดิมก่อนแล้วลองใหม่
- รายการส่งแล้วที่ยังไม่ลงยอดจะแสดงข้ามวันเพื่อไม่ให้ตกหล่น รายการเสร็จและยกเลิกแสดงเฉพาะวันไทยปัจจุบัน
- ตรวจรับเงินที่ เพิ่มเติม → ประวัติยอดขาย / รับเงิน ไม่บันทึกออเดอร์นี้ซ้ำด้วยฟอร์มขายเดิม
- ข้อมูลคิว refresh ทุก 15 วินาทีเมื่อเปิดหน้าจออยู่ ใช้สองเครื่องบนบัญชีร้านเดิมได้ การแยกบัญชีพนักงานยังไม่ได้เพิ่ม
- ยกเลิกได้ก่อนเริ่มทำเท่านั้น หลังเริ่มทำยังไม่มี flow ยกเลิก/บันทึกของเสียร่วมกับออเดอร์ รายการขายที่ผูกออเดอร์ไม่อนุญาตลบเพื่อป้องกันสต็อกและออเดอร์ไม่ตรงกัน

## ขอบเขต

ยังไม่เชื่อม Grab / LINE MAN API, ไม่ซิงก์เปิดปิดเมนูบนแพลตฟอร์ม, ไม่รองรับส่วนลด/settlement เต็มรูปแบบ, ยังไม่รองรับทำงาน offline หรือกู้แบบฟอร์มหลังปิดแท็บ และหน้าเดิมบางส่วนยังเป็นภาษาอังกฤษ

การตัดสต็อกใช้สินค้าพร้อมขายสำหรับสูตรผลิตเป็นรอบ และใช้วัตถุดิบตามสูตรสำหรับเมนูทำตามสั่ง พร้อมบันทึกปริมาณวัตถุดิบที่ใช้ใน transaction เดียวกัน เลือกประเภทสูตรให้ถูกก่อนลงยอด

## ตรวจสอบ

- `npx tsc --noEmit`
- `npx eslint src/app/today src/lib/orders.ts src/components/tab-bar.tsx src/app/more/page.tsx src/app/page.tsx src/proxy.ts`
- `node tests/delivery-orders.mjs` ใช้ PostgreSQL ในหน่วยความจำ ไม่เชื่อมร้านจริง: ตรวจสถานะ, เลขซ้ำ, rollback เมื่อสต็อกไม่พอ, retry ไม่สร้างยอดซ้ำ, สิทธิ์ข้ามผู้ใช้, การป้องกันลบยอดที่เชื่อมออเดอร์ และการลบยอดเดิม
- ทดสอบจริงหลังติดตั้ง: สร้างออเดอร์ 2 เมนู เปิดสองเครื่อง เปลี่ยนสถานะ เช็กแพ็ก ส่งของ ลงยอด และเทียบรายงาน/สต็อก รวมทั้งทดลองเครือข่ายขาดขณะกดบันทึก

ยังไม่ได้รัน migration กับฐานข้อมูลจริงหรือเผยแพร่ production
